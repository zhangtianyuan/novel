# 小说文件管理规则

> 入坑必读，不遵守就别怪我把文件给你删了。

---

## 目录结构

```
novel/
├── 规则.md          ← 这个文件
├── 正文.md          ← 小说正文，只有这一个正文文件
├── 梗概.md          ← 全篇梗概、分卷计划
├── 人物/            ← 人物设定，每人一个md
│   └── 陈坨.md
├── 伏笔.md          ← 伏笔跟踪
└── 设定/            ← 世界观、势力、地图等
    └── 世界观.md
```

## 文件规范

### 正文.md
- **只有这一个文件**。不许分拆。
- 分章节用 `## 第X章` 标题层级。
- 每章之间用 `---` 分隔。
- 旧章不改（除非你明确要求），只往后追加。

### 人物/ 目录
- 每人一个 .md 文件。
- 结构：基础信息 → 性格 → 能力 → 关系 → 当前状态。
- **改了这个文件，写小说时就按改后的性格来写。**

### 伏笔.md
- 🔵 待回收 / 🟡 进行中 / 🟢 已回收 / 🔴 废弃
- 埋了新伏笔就立刻追加，别忘。

### 梗概.md
- 总纲：每卷核心剧情一句话概括。
- 写之前看一眼，防止跑偏。

## 写作条约

1. **写之前读人物设定**：每次动笔前扫一遍人物 .md，按最新人设来。
2. **写之后更新伏笔**：新埋的坑立刻记到伏笔.md。
3. **不乱建文件**：正文只有 `正文.md`，不分拆。
4. **不乱改旧章**：旧章节除非你要求，否则不动。
5. **你随时可以改**：任何 .md 你都可以手动改，我写的时候自动认。
6. **文件永远是第一手设定**：我说了什么不重要，文件写了什么才重要。

---

> 简单来说：正文一个文件，人物一人一个文件，伏笔一个文件。谁也别乱建。

---

## 🔴 铁律：编码问题

1. **所有 .md 文件必须使用 UTF-8 编码，不带 BOM。**
2. **禁止使用 PowerShell 的 Add-Content 命令写入中文内容。** 该命令在中文 Windows 系统上会默认使用 GBK 编码，导致中文乱码。
3. **写入文件必须使用 Python 脚本**（`python -c "..."` 或 `novel_save.py`），Python 的 `open(path, 'w', encoding='utf-8')` 保证纯 UTF-8。
4. **git 提交前检查编码**：如果文件中有无法用 UTF-8 解码的字符，禁止提交。
5. **违反此规则导致乱码的，视为严重事故。**
